# Get the API's revision history.

Get the API's revision history.

## Usage

``` r
get_revision_history()
```

## Examples

``` r
if (FALSE) { # \dontrun{
result <- get_revision_history()
result$Data
} # }
```
