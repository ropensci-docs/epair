# Get any known issues within the API.

Get any known issues within the API.

## Usage

``` r
get_known_issues()
```

## Examples

``` r
if (FALSE) { # \dontrun{
result <- get_known_issues()
result$Data
} # }
```
